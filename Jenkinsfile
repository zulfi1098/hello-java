#!groovy

import groovy.json.JsonSlurper
import java.net.URL

pipeline {
 agent any
     stages {
                         stage ("build") {
                                     Steps {
                                                 echo "Build"
                                     }
                         }
            }
}